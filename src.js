let gamestate = "console"; //gamestate will be how the game knows what is going on. right now, it will be the default playing state,
                           //"console, for coding reasons."

while (gamestate = "console") {
    document.getElementById("inputline").focus();
}